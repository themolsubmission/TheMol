# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
tqdm>=4.60.0
lmdb>=1.0.0

# RDKit (install via conda recommended: conda install -c conda-forge rdkit)
# rdkit>=2022.03

# PyTorch (install separately based on CUDA version)
# See: https://pytorch.org/get-started/locally/
# torch>=2.0.0

# Uni-Core (install from pre-compiled wheel)
# See: https://github.com/dptech-corp/Uni-Core/releases
# unicore

# Optional: for optimization tasks
cmaes>=0.9.0
pyzmq>=22.0

# Optional: for evaluation
# posebusters
# xtb (install via conda: conda install -c conda-forge xtb)
